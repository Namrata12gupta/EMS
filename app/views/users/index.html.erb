<%= link_to "New User", new_user_path %>

<p align="right">	
<%= link_to 'Sign_out', destroy_user_session_path, data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
</p>

<h1>Users</h1>
<li>
	<%= link_to current_user.name, current_user %>

	<%= link_to "Destroy", user_path(@users), data: {
	    turbo_method: :delete,
	    turbo_confirm: "Are you sure?" } %>
</li>


