<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent"> 
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          	<% if user_signed_in? %>
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href= "<%= root_path %> ">Home</a>
            </li>

            <li class="nav-item">
              <a class="nav-link"  href="/users">Profile</a>
            </li>


            <li class="nav-item">
              <a class="nav-link" href="<%= "/users/:user_id/attendances/:id" %>">Attendance</a>
            </li>

            <li class="nav-item">
              <a class="nav-link" href="<%=daily_statuses_path %>">Daily Status</a>
            </li>

            <li class="nav-item">
              <a class="nav-link" href="<%= leaves_path(current_user.id) %>">Leave Status</a>
            </li>

            <li class="nav-item">
              <a class="nav-link" href="<%= "/users/:id/edit"%>">EditUser</a>
            </li>

            <%if current_user.role.role_type == 'HR' %>
             <li class="nav-item">
              <a class="nav-link" href="<%= hr_leaves_path %>">HR</a>
            </li>
            <% end %>

              <img style= "height: 3rem; wedith: 3rem; margin-left: 25rem;" <%=image_tag("simplly.jpeg")%> 

            <%= link_to 'Sign_out', destroy_user_session_path, data: {
                            turbo_method: :delete,
                            turbo_confirm: "Are you sure?"
                          } %>
            <% end %>
        
          </ul>
        </div>
        
  </div>
</nav>





























